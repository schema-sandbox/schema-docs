# Sample Fixtures

This directory tracks release sample workflows. Do not place sensitive real documents here.

For v0.1.0, fixtures can be synthetic, redacted, or generated during review. The important output is a repeatable record of which document exchange paths were checked and which limitations were observed.

Use `fixture-plan.json` as the source of truth for the minimum release gate.
