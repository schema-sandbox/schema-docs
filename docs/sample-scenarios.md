# Schema Docs Real Sample Scenario Map

> This file is generated by `npm run sample-scenarios` from the real sample result set. It maps known limits and edge-case files to the expected Schema Docs response: warning, confidence downgrade, Send Gate block, or safe handoff.

---

## Execution Summary

- **Total samples**: 20
- **AI-ready samples**: 9
- **Downgraded / blocked samples**: 11
- **Warning correctness**: all reviewed warning checks passed
- **AI-ready status**: 13 / 20

---

## Product Capability Classifications

Each sample is assessed against one or more core pipeline capabilities:
* **`ai_intake`**: Offline text extraction and layout flattening.
* **`safety_gate`**: Local-first PII masking and egress credential scanning.
* **`format_exchange`**: Bidirectional conversion and exchange packaging.
* **`table_filter`**: Local database queries to trim context.
* **`long_input`**: Context chunking and staged feeding plans.
* **`external_refresh`**: Out-of-band updates and version diffing.

---

## Scenario Matrix

| Sample | Fixture | Status | Quality | Send Gate | Capabilities | User Pain | System Handling | Next Action |
|---|---|---|---|---|---|---|---|---|
| RS-001: Academic PDF multi-column reading order | `academic-paper-multi-column.pdf` (PDF) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | MEDIUM | allowed after review | `ai_intake`, `safety_gate`, `format_exchange` | AI can read across columns in multi-column academic PDFs, mixing paragraphs and breaking formula context. | Schema Docs simplifies the layout into a single Markdown flow, marks the extraction as medium confidence, and asks the user to review reading order before sending. | Inspect affected paragraphs or tables in the Markdown working copy before sending the content through Send Gate. |
| RS-002: Financial report table extraction | `financial-report-complex-table.docx` (DOCX) | AI-READY AFTER REVIEW | HIGH | allowed after review | `ai_intake`, `format_exchange` | Nested or page-spanning Word tables can lose boundaries when reduced to plain text, making AI analysis unreliable. | The DOCX XML parser converts table cells into GitHub Flavored Markdown tables and keeps headings aligned where possible. | Use the extracted Markdown table as AI context or create an exchange package after a quick visual review. |
| RS-003: Digital legal agreement parsing | `legal-agreement-text.pdf` (PDF) | AI-READY AFTER REVIEW | HIGH | allowed after review | `ai_intake`, `safety_gate`, `format_exchange` | Legal PDFs often contain dense clauses where missing a disclaimer, amount, or date can create business risk. | Text-layer PDFs are extracted into Markdown with evidence hashes so reviewers can trace the source before AI analysis. | Run AI review rules only after confirming the AI Will See preview matches the source scope. |
| RS-004: Scanned survey or receipt upload | `patient-survey-scanned.pdf` (PDF) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | LOW | blocked for manual review | `safety_gate` | Image-only scanned PDFs can produce empty or unreadable text, causing AI to hallucinate from bad input. | Send Gate blocks low-confidence scanned input and reports that OCR is required before AI exchange. | Run OCR externally or provide a digital source file, then import the refreshed result. |
| RS-005: CJK employee list encoding compatibility | `chinese-employee-list.csv` (CSV) | AI-READY AFTER REVIEW | HIGH | allowed after review | `ai_intake`, `format_exchange`, `table_filter` | CSV files with mixed encodings can turn names into mojibake, causing entity extraction mistakes. | The importer validates CJK text handling and type inference, then reports high confidence when names remain readable. | Check the preview for unreadable characters before sending the table to AI. |
| RS-006: Multi-sheet inventory data selection | `inventory-records.xlsx` (XLSX) | AI-READY AFTER REVIEW | MEDIUM | allowed after review | `ai_intake`, `format_exchange`, `table_filter` | Sending an entire workbook wastes tokens and can distract AI with irrelevant sheets. | Schema Docs extracts a bounded sheet preview and records structured metadata so users can filter before AI context assembly. | Filter to the relevant inventory rows or sheet before loading table context into Send Gate. |
| RS-007: Formula-heavy spreadsheet risk | `formula-heavy-sheet.xlsx` (XLSX) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | LOW | blocked for manual review | `safety_gate`, `format_exchange`, `table_filter` | AI is not a spreadsheet calculation engine, and stale formula caches can lead to wrong financial answers. | Schema Docs extracts cached values only, warns that formulas are not executed, and blocks low-confidence direct sends. | Recalculate and save the spreadsheet in Excel or WPS before refreshing the import. |
| RS-008: Ultra-large CSV token overflow | `large-log-data.csv` (CSV) | AI-READY AFTER REVIEW | HIGH | allowed after review | `ai_intake`, `table_filter`, `long_input` | Sending hundreds of thousands of log rows directly to AI can exceed model context limits. | The system uses bounded previews, row-count metadata, and local query/filtering before any AI context is staged. | Use local query or export a narrowed table context instead of sending raw logs. |
| RS-009: Round-trip working copy consistency | `round-trip-docx-back-to-md.md` (MD) | AI-READY AFTER REVIEW | HIGH | allowed after review | `ai_intake`, `format_exchange`, `external_refresh` | Users may edit both the original Word file and the Markdown working copy, creating version ambiguity. | Schema Docs compares source hashes and Markdown edit state, writes refreshed extraction separately when needed, and keeps version history. | Use Refresh Source, inspect the risk preview, and choose whether to keep local Markdown edits or accept the refreshed extraction. |
| RS-010: CIDFont PDF mojibake detection | `cjk-corrupt-cidfont.pdf` (PDF) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | LOW | blocked for manual review | `safety_gate`, `format_exchange` | Some PDFs render correctly in a reader but expose broken CID font mappings during text extraction. | Schema Docs detects unreadable replacement characters, lowers confidence, and blocks unsafe Send Gate handoff. | Convert the PDF through Word/WPS or another trusted tool, then re-import a cleaner DOCX or text-layer PDF. |
| RS-011: Scanned medical receipt extraction | `scanned-medical-receipt.pdf` (PDF) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | LOW | blocked for manual review | `safety_gate` | Scanned receipts or medical printouts are low-contrast and have no text layer, so text extraction cannot produce trusted AI context. | The system detects the lack of a text layer, lowers confidence, warns that OCR is missing, and blocks direct AI intake. | Provide a high-fidelity digital text version or run OCR conversion externally before staging. |
| RS-012: Merged cells in invoice spreadsheet | `merged-cells-invoice.xlsx` (XLSX) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | MEDIUM | allowed after review | `ai_intake`, `table_filter` | Spreadsheets with merged header or data cells can break column alignment, causing AI to misinterpret key-value associations. | The system identifies merged cell structures and flattens them into clean, sequential header-column representations. | Review column layout in Send Gate preview to ensure aligned variables map correctly. |
| RS-013: Simple Word user guide | `simple-user-guide.docx` (DOCX) | AI-READY AFTER REVIEW | HIGH | allowed after review | `ai_intake`, `format_exchange` | Standard text manuals with clean headings, list items, and simple paragraphs need straightforward markdown conversion. | Direct DOCX XML parser maps structural tags to readable H2/H3 and list structures in Markdown for AI review. | Proceed directly to Send Gate preview and stage context. |
| RS-014: API spec document bookmarks | `api-spec-broken-links.pdf` (PDF) | AI-READY AFTER REVIEW | MEDIUM | allowed after review | `ai_intake`, `format_exchange` | API specifications contain interactive hyperlinks and bookmarks that are irrelevant for LLM textual understanding. | The parser extracts text layers and code blocks while dropping interactive styling and bookmark nodes. | Review generated endpoint text layout before staging. |
| RS-015: Large product catalog workbook | `large-product-catalog.xlsx` (XLSX) | AI-READY AFTER REVIEW | HIGH | allowed after review | `ai_intake`, `table_filter`, `long_input` | Spreadsheets with massive multi-sheet inventories can easily overflow token limits and waste model context. | Allows multi-sheet previewing and registers worksheet structures, guiding the user to select specific tabs and query rows. | Choose the target worksheet tab and apply local SQL filters before loading data to AI. |
| RS-016: Marketing deck embedded vector diagrams | `marketing-deck-embedded-images.pdf` (PDF) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | LOW | blocked for manual review | `safety_gate`, `format_exchange` | Presentation slides converted to PDF often contain rich shapes, flowcharts, and background layouts that cannot be represented in plain text. | Text extraction pulls textual layers only, lowering confidence for complex layouts and warning that diagrams are omitted. | Add manual summaries of critical diagrams into the Markdown working copy before sending. |
| RS-017: Complex payroll spreadsheet pivot table | `complex-payroll-pivot.xlsx` (XLSX) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | MEDIUM | allowed after review | `ai_intake`, `table_filter` | Pivot tables and nested matrix headers are highly structural and do not translate well to raw CSV. | Processes raw spreadsheet grids, flattens sub-headers into sequential fields, and flags a layout known limit. | Use local SQL query engine to filter down nested metrics or rows before staging. |
| RS-018: Handwritten survey form scanned PDF | `handwritten-feedback-form.pdf` (PDF) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | LOW | blocked for manual review | `safety_gate` | Handwritten text or ink marks do not have digital fonts, producing empty files under regular text extraction. | Detects empty character length, classifies the document as low confidence, and blocks sending to AI. | Run external OCR conversion or re-type critical feedback into the Markdown editor. |
| RS-019: Technical spec embedded raster illustrations | `technical-spec-images.docx` (DOCX) | DOWNGRADED BEFORE AI SEND (KNOWN LIMIT) | MEDIUM | allowed after review | `ai_intake`, `format_exchange` | Technical DOCX files contain embedded schematics or raster images that are skipped by text parsing. | The parser converts text and tables into Markdown, tags a layout known limit, and alerts that images are omitted. | Ensure that text descriptions are sufficient for AI understanding, or attach text explanations. |
| RS-020: Credential or API key leak prevention | `credentials-leak-blocked.txt` (TXT) | BLOCKED BEFORE AI SEND | HIGH | blocked for manual review | `safety_gate` | Accidentally staging plain-text AWS/OpenAI keys or DB passwords poses a severe security risk. | Send Gate security audit runs local CJK-aware patterns, detects credential strings, and blocks transmission. | Verify masked entities, clear leaked credentials from the source, and re-stage before sending. |

## Closing Note

Schema Docs is local-first and safety-boundary oriented. It does not promise perfect reproduction of every layout. Instead, it turns document intake into a controlled AI handoff: extract what can be trusted, downgrade confidence when needed, block unsafe sends, and preserve evidence for review.